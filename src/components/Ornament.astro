---
/**
 * The gold hairline with a centre diamond that separates sections.
 *
 * Purely decorative, so it is `aria-hidden` and carries no text: a screen
 * reader gets the section landmarks, which is the same information without the
 * ornament.
 */
---

<div class="ornament" aria-hidden="true"><i></i></div>
